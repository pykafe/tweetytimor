{% load i18n %}
{% load static %}
<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="{% static 'css/bootstrap.min.css' %}" />
        <link type="text/css" rel="stylesheet" media="screen" href="{{STATIC_URL}}css/biopy.css"/>
    
        <title> Hello Tweety </title>
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <h1>Hello Tweety Timor</h1>
            </div>
        </nav>
        <div class ="container">
            <p>say something...</p>
            <form action="." method="post">
                {% csrf_token %}
                <p>Add your {{ form.comment.label_tag }}...</p>
                <p> {{ form.comment}}</p>
                <p>{{ form.comment.errors }}</p>
                <div class = "row">
                    <div class="col-md-6">
                        <button id="submit">Add Comment</button>
                    </div>
                </div>
            </form>
        
            <ul>
                {% for tweet in tweets %}
                 <li>
                     <span>{{ tweet.comment }}</span>
                     <i>Created on {{ tweet.created_on }}</i>
                 </li>
                {% endfor %}
            </ul>

            <p>Total of the comment have {{total_count}}{% if total_count == 1 %} people {%elif total_count == 0 %} no comment {% else %} peoples {% endif %}</p>

        </div>
    </body>
</html>
